
<!--Bloc Carte entreprise-->
    {# <div class="card">
        <div class="card_bloc_1">
            <img src="{{ asset('images/announcements/'~ announcement.image )}}" alt="Image d'illustration de l'annonce : '{{ announcement.title }}'">
        </div>

        <div class="card_bloc_2">
            <div class="card_category pinkBackground">{{ announcement.category.name }}</div>
            <p>{{ announcement.activitySector.name }}</p>
            <div class="card_title">{{ announcement.title }}</div>
            <div class="card_text">{{ announcement.description }}</div>
            <div class="card_announcement">Annonce : {{ announcement.ref }}</div>
            <div class="keyWords"> Mots-clés :           
                {% for keyWords in announcement.keyWords %} 
                    #{{ keyWords }}  
                {% endfor %}
            </div>
        </div>

        <div class="card_bloc_3">
            <div class="informations">
                <div class="card_alternative_text"><i class="las la-map-marker"></i>Localisation : {{ announcement.deptCode }}</div>
                <div class="card_alternative_text"><i class="las la-clock"></i>Date de publication : {{ announcement.createdAt | date('d.m.Y')}}</div>
                <div class="card_alternative_text"><i class="las la-phone"></i>Telephone : {{ announcement.user.phone }} </div>
                <div class="card_alternative_text"><i class="las la-envelope"></i>Email : {{ announcement.user.email }}</div>
                <div class="card_alternative_text"><i class="las la-euro-sign"></i>Tarif : {{ announcement.priceRange[0] }} - {{ announcement.priceRange[1] }} &euro;</div>
            </div>

            <div class="buttonList">
                <a href="#" type="button" class=" button darkGreyBackground">Partager</a>
                <a href="#" type="button" class=" button darkGreyBackground">Enregistrer</a>
                <a href="#" type="button" class=" button darkGreyBackground">Recommander</a>
                <a href="#" type="button" class=" button lightGreyBackground">Contrepartie</a>
                <a href="#" type="button" class=" button violetBackground">Répondre</a>
            </div>
        </div>
    </div> #}
<!--Fin Bloc Carte entreprise-->  


<!--Bloc Carte entreprise-->
    <div class="card">
        <div class="card_bloc_1">
            <img src="{{ asset('images/announcements/'~ announcement.image )}}" alt="Image d'illustration de l'annonce : '{{ announcement.title }}'">
            {# <img src="{{ vich_uploader_asset(announcement, 'imageFile') | imagine_filter('miniatures') }}" alt="Image d'illustration de l'annonce : '{{ announcement.title }}'"> #}
        </div>

        <div class="card_bloc_2">
            <div class="card_category pinkBackground">
                <p>{{ announcement.category.name }}</p>
            </div>
            <p>{{ announcement.activitySector.name }}</p>
            <div class="card_title">
                <h2>{{ announcement.title }}</h2>
            </div>
            <div class="card_text">
                <p>{{ announcement.description }}</p>
            </div>
            <div class="card_announcement">
                <p>Annonce. {{ announcement.ref }}</p>
            </div>
            
            <div class="keyWords"> Mots-clés :  
                <p>
                    {% for keyWords in announcement.keyWords %} 
                        #{{ keyWords }}  
                    {% endfor %}                
                </p>         
            </div>
        </div>

        <div class="card_bloc_3">
            <div class="informations">

                {% set unlockedAnnouncement = false %}


                {% if app.user %}

                    {% for array in app.user.unlockedAnnouncements %}
                        {% if announcement.id == array.announcements.id %}
                            {% set unlockedAnnouncement = true %}
                        {% endif %}
                    {% endfor %}

                {% endif %}

                <div class="card_alternative_text">
                    <p><i class="las la-map-marker"></i>Localisation : {{ announcement.deptCode }}</p>
                </div>
                <div class="card_alternative_text">
                    <p>
                        <i class="las la-clock"></i> 
                        <time datetime="{{ announcement.createdAt | date('d.m.Y') }}">
                            {{ announcement.createdAt | date('d.m.Y')}}
                        </time>
                    </p>
                </div>
                <address>
                    <div class="card_alternative_text">
                        <p>
                            <i class="las la-phone"></i> 
                            <a href="tel:{{ ( unlockedAnnouncement == true ) ? announcement.user.phone : '#' }}">{{ ( unlockedAnnouncement == true ) ? announcement.user.phone : 'disponible' }}</a>                        
                        </p>

                    </div>
                    <div class="card_alternative_text">
                        <p>
                            <i class="las la-envelope"></i>
                            <a href="mailto:{{ ( unlockedAnnouncement == true ) ? announcement.user.email : '#' }}">{{ ( unlockedAnnouncement == true ) ? announcement.user.email : 'disponible' }}</a>                           
                        </p>
                    </div>                
                </address>
                <div class="card_alternative_text">
                    <p>
                        <i class="las la-euro-sign"></i> 
                        
                        {% if unlockedAnnouncement %}
                            {{ announcement.priceRange[0] }} - {{ announcement.priceRange[1] }} &euro;
                        {% else %}
                            disponible
                        {% endif %}                 
                    </p>

                </div>

                <p>Coordonnées débloquées {{ announcement.unlockCount }} fois.</p>

            </div>

            <div class="buttonList">
                <a href="#" type="button" class=" button darkGreyBackground">Partager</a>
                <a href="#" type="button" class=" button darkGreyBackground">Enregistrer</a>
                <a href="#" type="button" class=" button darkGreyBackground">Recommander</a>
                <a href="#" type="button" class=" button lightGreyBackground">Contrepartie</a>

                {# Ce boutton ouvre une modal bootsrap #}
                <button type="button" class=" button violetBackground" data-toggle="modal" data-target="#unlockAnnoncementModal">Répondre</button>

                <!-- Modal -->
                <div class="modal fade" id="unlockAnnoncementModal" tabindex="-1" role="dialog" aria-labelledby="unlockAnnoncementModalLabel" aria-hidden="true">
                    <div class="modal-dialog">
                        <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="unlockAnnoncementModalLabel">Modal title</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                            <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <p>Vous souhaitez être mis en relation avec ce client pour l’annonce : </p>
                            


                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-secondary" data-dismiss="modal">Close</button>
                            <button type="button" class="btn btn-primary">Save changes</button>
                        </div>
                        </div>
                    </div>
                </div>

                
            </div>
        </div>
    </div>
<!--Fin Bloc Carte entreprise-->  

