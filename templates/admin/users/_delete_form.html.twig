<form method="post" action="{{ path('admin_users_delete', {'id': user.id}) }}" onsubmit="return confirm('Etes-vous sûr ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn darkGreyBackground mt-3">Supprimer</button>
</form>
