<form method="post" action="{{ path('admin_activity_sector_delete', {'id': activity_sector.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activity_sector.id) }}">
    <button class="btn">Delete</button>
</form>
