{% extends 'admin/base_admin.html.twig' %}

{% block title %}ActivitySector{% endblock %}

{% block body %}
    <h1 class="pt-150">ActivitySector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Nom</th>
                <td>{{ activity_sector.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activity_sector_index') }}">Retour à la liste</a>

    <a href="{{ path('activity_sector_edit', {'id': activity_sector.id}) }}">Editer</a>

    {{ include('activity_sector/_delete_form.html.twig') }}
{% endblock %}
