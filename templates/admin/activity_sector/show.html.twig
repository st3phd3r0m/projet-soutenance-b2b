{% extends 'base.html.twig' %}

{% block title %}ActivitySector{% endblock %}

{% block body %}
    <h1>ActivitySector</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ activity_sector.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ activity_sector.name }}</td>
            </tr>
            <tr>
                <th>Slug</th>
                <td>{{ activity_sector.slug }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('activity_sector_index') }}">back to list</a>

    <a href="{{ path('activity_sector_edit', {'id': activity_sector.id}) }}">edit</a>

    {{ include('activity_sector/_delete_form.html.twig') }}
{% endblock %}
