<!DOCTYPE html>
<html lang="fr">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title> Yeah! - Votre commande </title>
    <style type="text/css">
        {{ fileGetContents( '../public/css/styles.min.css' )  }}
        {{ fileGetContents( '../public/css/stylesPDF.min.css' ) }}
    </style>
</head>

<body>
    <main>

        <div>
            <h1> Yeah! - Facture client </h1>
            <img src="{{ base64 }}">
        </div>
        

        <h2>Informations client</h2>
        {# Début tableau #}
        <table>
            {# DEBUT THEAD #}
            <thead>
                <tr>   
                    <th scope="col">Client</th> 
                    <th scope="col">Date de la commande</th>
                </tr>
            </thead>
            {# FIN THEAD #}


            {# DEBUT TBODY #}
            <tbody>
                <tr>   
                    <td>{{ app.user.firstname }} {{ app.user.lastname }}</td> 
                    <td>{{ subscriptionPurchase.orderedAt | date('d.m.Y')  }}</td>
                </tr>
            </tbody>
            {# FIN TBODY #}
        </table>

        <h2>Articles commandés</h2>
        {# Début tableau #}
        <table>
            {# DEBUT THEAD #}
            <thead>
                <tr>
                    <th scope="col">Nom du produit</th>
                    <th scope="col">Prix unitaire</th>
                    <th scope="col">Quantité</th>
                    <th scope="col">Montant</th>
                </tr>
            </thead>
            {# FIN THEAD #}

            {# DEBUT TBODY #}
            <tbody>

                <tr>
                    <td>{{ subscriptionPurchase.subscription.name }}</td>    
                    <td>{{ subscriptionPurchase.subscription.price }} &euro;</td>
                    <td>{{ subscriptionPurchase.quantity }}</td>
                    <td>{{ subscriptionPurchase.amount }} &euro;</td>
                </tr>
                <tr>
                    <th colspan="2"></th>
                    <td>
                        <span class="font-weight-bold">Montant total à régler : </span>
                    </td>    
                    <td><span class="font-weight-bold">{{ subscriptionPurchase.amount }} &euro;</span></td>
                </tr>

            </tbody>
            {# FIN TBODY #}
        </table>

    </main>
</body>

</html>


